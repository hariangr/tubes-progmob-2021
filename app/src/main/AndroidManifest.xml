<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.aryad.sipasar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PembayaranGaji">
        <activity
            android:label="Detail Histori Gaji"
            android:name=".DetailHistoryGajiActivity"
            android:parentActivityName=".AturGajiActivity"></activity>
        <activity android:name=".AddHistoryGajiActivity" />
        <activity
            android:name=".AturGajiActivity"
            android:label="Atur Gaji"
            android:parentActivityName=".BayarGajiActivity" />
        <activity
            android:name=".BayarGajiActivity"
            android:label="@string/title_activity_bayar_gaji"
            android:theme="@style/Theme.PembayaranGaji.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="Sipasar Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>